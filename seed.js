const db = require('./models');
const model_list = [

];

/*db.Model.deletemany({}, (err, models) => {
    if (err) {
        console.log(`Seed.js population error: ${err}`);
        return;
    }
    console.log(`Recreated all models`);
    console.log(`Created ${models.length} models`;
    mongoose.connection.close();
    });
 }); */